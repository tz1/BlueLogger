There is a BlueLogger.apk in the bin directory

This is an extension to the Android VT100 emulator for serial Bluetooth, BlueTerm, adding: 

gzipped logging of what is received in the downloads directory as BT-(time).txt.gz
Get SkyTraq AGPS from ftp to download directory
(not quite yet) upload AGPS to SkyTraq Venus chip.
Reconnect (one attempt) if the disconnect was not done by tapping disconnect.

